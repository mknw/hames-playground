{
  "name": "@hames/sandbox",
  "version": "0.1.0",
  "description": "The containment companion for @hames/harness-patterns — withSandbox, the Docker compute backend, the egress profiles and the bash guard, behind injected host seams.",
  "license": "MIT",
  "type": "module",
  "main": "./index.ts",
  "exports": {
    ".": "./index.ts",
    "./package.json": "./package.json",
    "./guard": "./bash-guard.ts",
    "./*": "./*.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "prepublishOnly": "node -e \"const ua=process.env.npm_config_user_agent||'';const ex=process.env.npm_execpath||'';if(ua.startsWith('npm/')||ex.includes('npm-cli'))throw new Error('This package must be published with pnpm publish, not npm: npm does not rewrite workspace: specifiers and ships them literally.')\""
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "node-pty": "^1.1.0"
  },
  "peerDependencies": {
    "@hames/harness-patterns": "workspace:^"
  },
  "devDependencies": {
    "@hames/harness-baml": "workspace:^",
    "@hames/harness-patterns": "workspace:^",
    "@vitest/coverage-v8": "^4.1.5",
    "vitest": "^4.1.5"
  },
  "files": [
    "*.ts",
    "!vitest.config.ts",
    "LICENSE",
    "README.md"
  ],
  "publishConfig": {
    "access": "public"
  }
}
