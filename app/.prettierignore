# Generated — never hand-formatted. Kept deliberately although `app/baml_client/`
# can no longer exist (the one corpus and its committed client live in
# `packages/harness-baml/`): it costs nothing and covers a stale local copy.
# See PR #376 §11.
baml_client/

# Build output
.output/
.vinxi/
dist/
coverage/

# Not ours
node_modules/
pnpm-lock.yaml
