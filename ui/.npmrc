# `predev` / `predev:exposed` regenerate baml_client before the dev server
# starts (#154 — a stale client silently drops the collector). pnpm 10 runs
# pre/post scripts by default, but the setting is opt-outable per user, and a
# dev who turned it off would lose the guard silently. Pin it here.
enable-pre-post-scripts=true
